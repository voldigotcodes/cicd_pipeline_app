language: python
python:
  - "3.10"

install:
  - pip install -e .
  - pip install -r requirements.txt

script:
  - pytest

deploy:
  provider: pypi
  user: __token__
  password: $PYPI_TOKEN
  distributions: sdist bdist_wheel
  on:
    tags: true
